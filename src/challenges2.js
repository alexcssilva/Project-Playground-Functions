// Desafio 10
function techList(techNames, name) {
  let techNames = [];
  let objectNames = '';
  let name = ''

  for (techs of techNames) {
    if (tech < techNames) {
      objectNames = {
        tech: "NomeTech",
        name: name,
      }
      console.log()
    }
  }

}
console.log(techList(["React", "Jest", "HTML", "CSS", "JavaScript"],'Lucas'));
// Desafio 11
function generatePhoneNumber() {
  // seu código aqui
}

// Desafio 12
function triangleCheck() {
  // seu código aqui
}

// Desafio 13
function hydrate() {
  // seu código aqui
}

module.exports = {
  generatePhoneNumber,
  techList,
  hydrate,
  triangleCheck,
};
